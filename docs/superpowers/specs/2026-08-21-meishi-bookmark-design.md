# NEXUA名刺ポケット 設計仕様（初版）

正式名称: **NEXUA名刺ポケット**（作業用コードネーム/リポジトリ名は `meishi-bookmark` のまま。URL変更を避けるため、表示名のみこの名称に変更）

## 概要・目的

NEXUA（デジタル名刺サイト、`nexua.tech`）で名刺を見せられた人が、受け取った名刺URLを後からまとめて見返せるようにするための、NEXUAとは独立した新しいWebアプリ。

## 背景・課題

- 電子名刺を受け取っても、後で見返す方法が分かりにくい
- 「ホーム画面にアイコン追加」で対応しようとすると、複数人と名刺交換した場合（例: 10人なら10個）ホーム画面が埋まってしまい管理しきれない
- 一つにまとまった場所で、受け取った名刺を一覧管理できる仕組みが必要

## 対象ユーザー

NEXUAユーザーに限らない。**名刺を見せられただけの一般の人**（NEXUAのアカウントを持たない人）も利用できることが必須条件。

## 全体アーキテクチャ

```
[NEXUAの名刺画面] --「保存」ボタン--> [NEXUA名刺ポケット（新規タブ）]
                                              ↓ Googleログイン
                                    [お客様自身のGoogle Drive]
                                    （専用シートに自動追記）
                                              ↓
                                    [一覧画面がそのシートを読んで表示]
```

- NEXUA側の変更は最小限：名刺画面に「保存」ボタンを1つ追加するだけ
- 新しいアプリはNEXUAの外側の、完全に独立した静的サイトとして作る（サーバー不要、GitHub Pages等でホスト可能）
- 認証・データ保存は、Googleが提供する仕組み（Googleログイン＋Google Sheets）だけで完結し、運営(NEXUA)側のサーバーやデータベースは一切増やさない

## 技術方針の選定

「お客様自身のGoogleアカウントで」という要望の背景は、**運営(NEXUA)側のサーバー・管理負担を増やしたくないこと**、および**誰が誰の名刺を保存したかという交友関係をNEXUA運営が把握したくない（プライバシー配慮）こと**の両方。

検討した3案：

| 案 | 概要 | 評価 |
|---|---|---|
| **A（採用）** | お客様のGoogle Driveに個人用シートを自動作成 | 運営負担・プライバシーの両方を追加インフラなしで満たす。既存NEXUA(GAS)と同じGoogleエコシステムで技術的にも親和性が高い |
| B | ブラウザのローカル保存のみ、Googleログインは本人確認程度 | 実装は一番簡単だが、端末を変えるとデータが消え「クラウドで管理」という趣旨から外れる |
| C | 新しいクラウド基盤（Firebase等）を新設 | 拡張性は高いが、運営側の新しい管理対象が増え「運営負担を増やしたくない」という要望と逆行する |

→ **案Aを採用**（初版。後述「v2改訂」で案Bとのハイブリッドに変更）

## v2改訂: 認証不要ローカル保存を基本に（2026-08-21）

初版実装後、「慣れていない高齢女性など、何タップもしたり入力したりする操作が負担になる利用者がいる」という指摘を受け、保存フローを見直した。

- **基本（常時）**: 「保存」を押すと**認証なしで即座にその端末のブラウザ内（localStorage）に保存**。1タップで完結し、「保存しました」と表示して終わる
- **オプション（希望する人だけ）**: 一覧画面に「Googleでバックアップ・他の端末と同期する」というボタンを設置。押した人だけGoogleログインに進み、以後は案Aの仕組み（お客様のGoogle Drive内シート）と同期する。初回連携時、それまでローカルに貯まっていたデータもまとめてアップロードする

つまり案B（ローカル保存）を土台にし、案A（Google Drive）を任意のアップグレードとして重ねるハイブリッド構成。運営負担・プライバシー配慮という初版の要件は維持しつつ、初回体験の負担を最小化した。

## 保存フロー（v3）

1. 名刺閲覧者が「保存」ボタンをタップ
2. 新しいタブでブックマークアプリが開く（名刺URL・表示名・タグをクエリパラメータで渡す）
3. **認証なしで即座に**その端末のlocalStorageに1行保存
4. 「保存しました」と、任意入力の「ひとことメモ」欄（会った場所など）、一覧画面（`?`パラメータなしのトップURL）への「保存した名刺の一覧を見る」ボタンを表示
5. メモは「メモを保存」を押した時点で該当エントリに追記される（保存自体はメモの入力有無に関わらずステップ3で完了済み。メモは任意のオプトイン要素）

## 一覧・バックアップフロー（v2）

1. 一覧画面を開くと、ログイン不要でこの端末のローカルデータをすぐ表示
2. 「Googleでバックアップ・他の端末と同期する」ボタンをタップした人だけGoogleログイン画面へ
3. ログイン後、お客様のGoogle Drive内で「NEXUAブックマーク」という専用シートを自動検索。なければ自動作成
4. 初回連携時のみ、ローカルに貯まっていた全件をそのシートへ一括アップロード
5. 以後、一覧表示はGoogle Sheets側の内容に切り替わる（同じ端末で連携済みなら再訪時もこの表示）

## 保存データ項目

| 項目 | 内容 |
|---|---|
| 名刺URL | 保存対象の名刺ページURL |
| 表示名 | 相手の名刺に表示されている名前 |
| タグ | NEXUAのタグ情報（配列、後でタグ絞り込みに使う） |
| 保存日時 | 保存した日時 |
| ひとことメモ | 会った場所など、任意入力のテキスト（v3で追加。保存完了画面での初回入力に加え、v4で一覧画面の各カードからいつでも編集可能に） |
| 写真 | メモ編集欄から追加できる任意の写真（Google連携済みの場合のみ）。Driveへアップロードしたファイルのリンク先URLをシートに保存する |
| 表面写真・裏面写真 | 「紙の名刺を登録」で撮影した写真（Google連携済みの場合のみ）。名刺URLの代わりにこれが本体となるエントリ |

## 重複対策

初期バージョンでは行わない。同じ人を誤って2回保存しても、両方保存される（YAGNI、後で必要になれば追加検討）。

## 一覧画面

- カード型のリスト表示（表示名・保存日・タグ・メモ）
- カードをタップするとその人の名刺URLが開く
- 検索は名前・タグ・メモを横断（v3で拡張）、保存日順の並び替え
- 各カードから削除可能
- タグは後から編集可能（ローカル・Google同期どちらのモードでも対応。NEXUA名刺・紙の名刺どちらも編集できる）
- Google連携済みの場合、「Googleスプレッドシートで開く」リンクを表示（そのまま元データのSheetsを直接開き、並べ替え・フィルタ・他の人との共有ができる）
- Google連携済みの場合、「📇 紙の名刺を登録」ボタンを表示。紙の名刺の表面（必須）・裏面（任意）を撮影/選択し、名前・タグ・メモ（任意、いずれも手入力）とあわせて登録する。名前のOCR自動読み取りは行わない（無料・サーバーなしの方針を維持するため見送り。Tesseract.jsは無料だが名刺の名前欄は精度が不安定、高精度なCloud Vision APIは課金・キー管理が発生し方針と合わない）。NEXUA名刺URLの代わりに表面写真がカードのリンク先になる
- **ネットワーク図**（保存した人同士のつながりを図で可視化）は初期バージョンでは作らない。ただしタグ・保存日時をデータとして持たせているため、後から追加しやすい設計にしておく

## エラーハンドリング

- Googleログイン失敗 → バックアップボタンを再度押せる状態に戻す
- シート書き込み失敗（回線エラー等） → 「Google連携に失敗しました。もう一度お試しください」と表示（ローカル保存自体は失敗しない）

## 非対象（YAGNI）

- 重複保存の防止
- ネットワーク図表示（初期バージョンでは対象外、データ構造上は拡張可能に留める）
- 複数端末間のリアルタイム同期の独自実装（Google Sheetsの標準的な同期挙動に依存する）
- OAuth審査申請によるGoogle同意画面の本番公開（テストユーザー限定運用のまま。必要性を見て別途判断）

## 未決定・実装計画フェーズで詰める事項

- 具体的なデザイン（NEXUAと統一感を持たせるか、独自ブランドにするか）
- Google Sheets API / Google Identity Servicesの認証スコープと具体的な実装方法
- NEXUA側「保存」ボタンの配置場所（`flipcard.jsx`のどの位置に置くか）

## 変更履歴

- 2026-08-21: 初版作成
- 2026-08-21: v2改訂。認証不要のローカル保存を基本フローに変更し、Google連携（案A）は一覧画面から任意で選べるオプションに位置づけ直した（commits c8f32d9, 4a68cc4）
- 2026-08-21: 正式名称を「NEXUA名刺ポケット」に決定（リポジトリ名/公開URLはmeishi-bookmarkのまま維持）
- 2026-08-21: v3。保存完了画面に任意入力の「ひとことメモ」欄を追加。localStorage・Googleシート同期（列E）の両方にmemoフィールドを追加
- 2026-08-21: 一覧画面の検索を名前のみからタグ・メモも含む横断検索に拡張
- 2026-08-22: v4。一覧画面の各カードから「ひとことメモ」を後から編集できるように（ローカル・Google同期どちらのモードでも対応。Google側はE列を直接更新するupdateMemoを追加）
- 2026-08-22: 不具合修正。既にGoogle連携済みの状態で再同期すると、ローカルでの新規保存・メモ編集がGoogle側の古いデータでの上書き表示により消えて見えていた。再同期のたびにurlをキーにローカル→リモートへ差分反映（新規はappend、メモの食い違いはupdateMemo）してから取得するように変更
- 2026-08-22: 不具合修正。Google同期後の画面で削除・メモ保存が失敗する不具合の原因調査。findSheetがGoogleシートの実際のgid(sheetId)を決め打ち0にしていたバグを修正（実際のsheetIdを取得するように変更）。あわせて、削除・メモ保存失敗時にアクセストークン期限切れ(401/403)を検知し「もう一度同期してから試す」よう案内するメッセージに変更
- 2026-08-22: 「Google連携済み端末での自動（サイレント）再ログインはしない」というYAGNI方針を撤回。一覧画面を開いた時、連携済み端末なら`requestSilentLogin`（Google Identity Servicesの`prompt: ''`）でポップアップなしのトークン再取得を自動試行するように変更。失敗時（Cookie制限・同意取り消し等）は従来どおり手動タップにフォールバック
- 2026-08-22: Google同期完了時、一番上の見出しを「Google by NEXUA名刺ポケット」に切り替え、背景色も暖色系からブルー系に変更。あわせて「Googleスプレッドシートで開く」リンクを追加（元データのSheetsを直接開いて並べ替え・共有ができる）。confirm()/alert()を自前モーダル(appConfirm/appAlert)に置き換え、URLが表示されないように変更
- 2026-08-22: 各カードに「連絡先に追加」ボタンを追加。People API(`people:createContact`)で名前・URL・タグ・メモをGoogle連絡先へ登録。`contacts`スコープ(機密スコープ、要OAuth同意画面へのスコープ登録)はバックアップ用トークンとは別クライアントで、初回タップ時にだけ同意を求める設計に
- 2026-08-22: 上記「連絡先に追加」機能を撤去（活用度が見込めないと判断）。src/contacts.js・関連UI・auth.jsのcontacts用トークンクライアント・README/仕様書のcontactsスコープ記載を削除
- 2026-08-22: Google連携済みのカードのみ、メモ編集欄から写真を追加できるように（カメラ起動 or ファイル選択）。canvasで縮小・JPEG圧縮してからDriveへアップロードし、「リンクを知っている人は閲覧可」に設定してカードにサムネイル表示する。新スコープ不要（既存のdrive.fileで作成したファイルの範囲内のため）。シートにF列(photoUrl)を追加、src/drive.js(uploadPhoto)・sheets.jsのupdatePhotoUrlを新設
- 2026-08-22: 不具合修正。サムネイルが表示されない・枠が大きすぎる問題を修正。写真URLを`uc?export=view`から`<img>`表示が安定するDriveのサムネイル専用エンドポイント(`drive.google.com/thumbnail`)に変更し、サムネイルのCSSサイズを72x72pxの小さい正方形に縮小
- 2026-08-22: Google連携済みの場合のみ「紙の名刺を登録」機能を追加。表面（必須）・裏面（任意）を撮影/選択し、名前・タグ・メモとあわせて新規行として登録する（URLなし、写真がカードのリンク先になる）。シートにG列(frontPhotoUrl)・H列(backPhotoUrl)を追加。名前のOCR自動読み取りは、無料・サーバーなしの方針とのトレードオフを検討した結果、今回は見送り（手入力）
- 2026-08-22: 不具合修正（重要）。紙の名刺登録で、名前・メモ・サムネイルが表示されない不具合。原因はGoogle Sheetsのvalues.append APIが「既存の表の幅」を自動検出して書き込み列を決める仕様で、G/H列を初めて使うシート（ヘッダーが更新されずA〜D等の古い列数のままだったシート）で表の幅の誤検出が起き、データが本来と異なる列にズレて書き込まれていた。appendBookmarkをappend APIから「A列のデータ行数を数えて次の行番号へ直接PUT」する方式に変更し、列ズレを構造的に防止。あわせてfindSheetで既存シートを見つけた際、ヘッダーが古い（列数不足）なら最新のHEADER定数で上書きするensureHeaderを追加
- 2026-08-22: タグを後から編集できるように（NEXUA名刺・紙の名刺どちらも、ローカル・Google同期どちらのモードでも対応）。memo編集と同じUIパターンで実装（storage.jsのupdateLocalTags、sheets.jsのupdateTags(C列)を新設）。紙の名刺の表裏サムネイルを正方形から横長(16:10)に変更し、撮影モーダルに「スマホを横向きで撮影」の案内文を追加
- 2026-08-22: 不具合修正（重要）。連携済み端末で一覧画面を開き直すたびに、未連携の見た目に一瞬（または失敗時はそのまま）戻る不具合を修正。まず起動時の見た目をisGoogleLinkedに応じて即座に出し分けるように変更（チラつき解消）。次に実機検証で、requestSilentLoginのcallbackが一度も呼ばれずに画面が固まるケースを発見し、4秒でタイムアウトして手動フォールバックする仕組みを追加。さらに実機（iPhone Safari）で「開き直すたびにGoogleのアカウント選択画面が強制的に表示される」現象を確認し、これがモバイルSafariでprompt:''によるサイレント取得がサイレントに完結しない既知の制約によるものと特定。根本対策として、GISから受け取ったアクセストークンをlocalStorage(meishi_google_token_cache)に有効期限（expires_inから60秒マージンを引いた時刻）付きでキャッシュし、キャッシュが有効な間はrequestSilentLogin自体を呼ばずキャッシュ済みトークンでそのままデータ取得するように変更。これにより連携直後〜約1時間はアカウント選択画面が出ずに開ける。有効期限切れ後は従来どおり手動タップでの再連携が必要（Googleがリフレッシュトークンを提供しない設計のため、サーバーなし構成での自動更新はできない）
- 2026-08-22: 設計変更（重要）。上記のrequestSilentLoginは、実機（iPhone Safari）で4回に1回程度の頻度でアカウント選択画面を強制的に表示してしまうことが再現し、トークンキャッシュを組み合わせても頻度が下がるだけで解消しなかった。別解として「GAS Web Appをアクセスした本人の権限で実行するモード」を試すため、認証の安定性を検証する小規模なGASプロジェクト(gas-spike、後日削除)を試作したが、テスト運用中のOAuthアプリ特有のテストユーザー管理・Google Cloudプロジェクトの権限管理（複数Googleアカウントをまたぐ検証環境のセットアップ）で複数回行き詰まり、検証コストがこのアプリの「サーバーなしで完結する」という方針に見合わないと判断して中断した。最終的にrequestSilentLogin自体を廃止（auth.jsから削除）。トークンキャッシュ（有効期限内は自動でスムーズ）はそのまま活かし、キャッシュが無い/切れている場合はGoogleへ自動で問い合わせず、「タップしてGoogle側の一覧を表示」ボタンを表示するだけに留める設計に統一。これにより、ページを開いただけで勝手にGoogleのアカウント選択画面が表示されることはなくなった（トレードオフとして、トークン失効後は毎回ワンタップでの再連携が必要）
- 2026-08-22: 不具合修正（重要）。Google連携済みの状態で一覧から名刺を削除すると、Google側のスプレッドシートからは消えるが、この端末のローカルデータ(localStorage)からは消えないままだった不具合を修正。この状態を放置すると、次回同期時（差分同期は「ローカルにあってGoogle側にない分をアップロード」というロジックのため）、削除したはずの名刺がGoogle側に復活してしまう。storage.jsにurlをキーにローカルエントリを削除するdeleteLocalByUrlを追加し、Google連携済みモードでの削除時にGoogle側の削除と同時にローカル側も削除するように修正
- 2026-08-22: 不具合修正（重大・データ損失あり）。appendBookmarkの行番号計算(getNextRowIndex)が、A列(url)だけを見て既存データ行数を数えていたため、紙の名刺（urlを持たない、A列が空）が存在すると、Google Sheets APIがその行を「存在しない」ものとして返し、行数を実際より少なく数え漏らしていた。その結果、次に追加する名刺のデータが、紙の名刺（またはそれ以降）の行にPUTで上書きされ、既存データが消えてしまう事故が実際に発生した。ユーザー報告（「紙の名刺が表示されない」「スプレッドシートを見ていたら行が消えた」）から発覚。修正: getNextRowIndexの取得範囲をA:AからA2:H（複数列、savedAt列は常に埋まる）に変更し、いずれかの列に値がある行を正しく数えるように修正。再発防止のsheets.test.jsテストケースを追加
